void print(int count, int n,vector<string>&ans){
	if(count > n) return;
	ans.push_back("Coding Ninjas");

	print(count+1,n,ans);
}

vector<string> printNTimes(int n) {
	// Write your code here.
	
	vector<string>ans;
	print(1,n,ans);

	return ans;;
}
